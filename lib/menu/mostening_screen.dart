import 'package:flutter/material.dart';

class MosteningScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mostening')),
      body: Center(child: Text('Mostening Sayfası')),
    );
  }
}