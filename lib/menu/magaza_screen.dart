import 'package:flutter/material.dart';

class MagazaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mağaza')),
      body: Center(child: Text('Mağaza Sayfası')),
    );
  }
}