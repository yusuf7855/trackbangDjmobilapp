import 'package:flutter/material.dart';

class BizKimizScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Biz Kimiz')),
      body: Center(child: Text('Biz Kimiz Sayfası')),
    );
  }
}